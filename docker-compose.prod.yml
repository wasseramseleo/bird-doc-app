services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: birddoc
      POSTGRES_USER: birddoc
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - /opt/bird-doc-app/pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U birddoc -d birddoc"]
      interval: 10s
      timeout: 5s
      retries: 6

  backend:
    image: ghcr.io/wasseramseleo/bird-doc-app-backend:latest
    restart: unless-stopped
    env_file:
      - /opt/bird-doc-app/.env
    volumes:
      - staticfiles:/app/staticfiles
    depends_on:
      db:
        condition: service_healthy

  frontend:
    image: ghcr.io/wasseramseleo/bird-doc-app-frontend:latest
    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    # Public ingress (ADR 0007 — no Cloudflare Tunnel). Caddy terminates TLS via
    # Let's Encrypt: 80 carries the ACME HTTP-01 challenge + HTTP->HTTPS redirect,
    # 443 serves traffic (tcp + udp for HTTP/3). Hostnames are baked into the
    # Caddyfile, so no DOMAIN env is needed.
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - /opt/bird-doc-app/Caddyfile:/etc/caddy/Caddyfile:ro
      - /opt/bird-doc-app/caddy_data:/data
      - /opt/bird-doc-app/caddy_config:/config
      - staticfiles:/srv/staticfiles:ro
    depends_on:
      - backend
      - frontend

volumes:
  staticfiles:
