import { SelectOnTab } from './select-on-tab';

describe('SelectOnTab', () => {
  it('should create an instance', () => {
    const directive = new SelectOnTab();
    expect(directive).toBeTruthy();
  });
});
