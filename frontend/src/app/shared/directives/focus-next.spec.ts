import { FocusNext } from './focus-next';

describe('FocusNext', () => {
  it('should create an instance', () => {
    const directive = new FocusNext();
    expect(directive).toBeTruthy();
  });
});
