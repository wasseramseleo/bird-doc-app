import { Routes } from '@angular/router';
import {DataEntryFormComponent} from './data-entry-form/data-entry-form';

export const routes: Routes = [
  { path: '', component: DataEntryFormComponent },
];
